Test example for using node js

Just locally (sorry =))

If you want to check:
clone this, install packages, setup and run 'mongod' and 'yarn build-start'.

The structure could be better, now it for simple usage only
